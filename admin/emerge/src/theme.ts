import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  // Theme will be configured during migration to match CBP theme
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});
