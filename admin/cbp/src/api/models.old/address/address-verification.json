

{
  "AddressVerificationSettings": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "provider": {
        "type": "string"
      },
      "verificationLevel": {
        "type": "string",
        "enum": ["None", "Basic", "Full"]
      },
      "retryAttempts": {
        "type": "integer"
      }
    }
  }
}