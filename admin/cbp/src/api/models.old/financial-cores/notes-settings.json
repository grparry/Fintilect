

{
  "NotesSettings": {
    "type": "object",
    "description": "General settings for notes functionality across financial cores",
    "properties": {
      "addressManagement": {
        "type": "object",
        "description": "Settings for address-related note management",
        "properties": {
          "clearBadAddressFlagOnNoChange": {
            "type": "boolean",
            "description": "Whether to clear the bad address flag when no changes are made"
          }
        },
        "required": ["clearBadAddressFlagOnNoChange"]
      }
    },
    "required": ["addressManagement"]
  }
}