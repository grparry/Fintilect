{
  "CheckImageOutputTypes": {
    "type": "string",
    "description": "Available check image output types. Mirrors XMLItemMobileDepositImageFormatType with the addition of TIFF and PDF. Vertifi returns images as URL when we request a PDF",
    "enum": [
      "JPEG",
      "PNG",
      "URL",
      "TIFF",
      "PDF"
    ]
  }
}
