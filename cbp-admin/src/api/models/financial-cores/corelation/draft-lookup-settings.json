{
  "DraftLookupSettings": {
    "type": "object",
    "description": "Settings for draft lookup functionality in Corelation integration",
    "x-legacy-model": "Psi.Data.Models.ClientConfigurationModels.FinancialCores.CorelationSettings.DraftLookup",
    "properties": {
      "accountNumberLength": {
        "type": "integer",
        "description": "Required length of account numbers for draft lookup",
        "minimum": 1,
        "nullable": true,
        "x-setting-key": "FinancialCore.Corelation.DraftLookup.AccountNumberLength"
      }
    },
    "required": ["accountNumberLength"]
  }
}
